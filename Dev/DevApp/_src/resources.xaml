<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:DevApp"                    
                    xmlns:v="clr-namespace:DevApp.Views"
                    xmlns:vm="clr-namespace:DevApp.ViewModels"
                    xmlns:tc="clr-namespace:MoellonToolkit.CommonDlgs.Impl.Components;assembly=MoellonToolkit.CommonDlgs.Impl">

    <!-- define in the app -->
    <DataTemplate DataType="{x:Type vm:EditDynDataGridVM}">
        <v:EditDynDataGridView />
    </DataTemplate>

    <DataTemplate DataType="{x:Type vm:MultiComponentsVM}">
        <v:MultiComponents />
    </DataTemplate>


    <!-- declare this for the dynamic dataGrid component 
         here or in the component View using the component -->
    <!--<DataTemplate DataType="{x:Type tc:DynDataGridVM}">
        <tc:DynDataGridView />
    </DataTemplate>-->
</ResourceDictionary>